# Handoff 2026-09-16 (Fable → Astra and Opus)

Written by Fable at the owner's request so the work of the day can continue
without this session. Everything below is verified unless marked otherwise.
Read `docs/ROADMAP-STATUS-2026-09-16.md` (what the roadmap holds at the
launch), `DECISIONS.md` (entries dated 2026-09-16) and
`docs/PRODUCTION-CHECKLIST.md` before picking anything up.

## State of main and the open branch

Merged today, on staging and in production (production ran migrations up to
`20260916460000` this morning; later ones are pending a dispatch):

- Free core (PR 204) then replaced by open core and AI credits (PR 206,
  `20260916440000`), docs/PRICING.md.
- Hosted MCP connector (PR 205, `20260916430000`), docs/HOSTED-MCP.md.
- Production deploy with the Vercel CLI after the database (PR 209).

Open: **PR 210 "Eight product languages"** (branch `fable/languages`):
migration `20260916470000`, dictionaries `messages/{no,dk,fi,de,es,it}.json`,
`lib/i18n.ts`, selectors, tests. Its last CI run failed only on three e2e
steps that clicked the old "English" button on the login page; the branch
now selects from the new language dropdown (`.locale-switch select`). The
local run of those three tests had not finished when this note was written.

**First task:** wait for CI on PR 210, merge it with the usual chain (rebase
on main if needed, `platform` check green, merge, delete branch). If an e2e
step still fails, look for tests that assume Swedish and English are the
only languages: every such assumption found so far was changed to
Portuguese (`pt`) as the unsupported example.

**Second task:** after the merge, dispatch `Production database` on main in
GitHub Actions (the owner approves the environment). It applies
`20260916470000` and deploys the application with the Vercel CLI.

## Production (live since today)

- Supabase `vpjpnqpkdejwzwpjjoon` (Stockholm, small compute, PITR 7 days),
  Vercel project `komisio-production` (team komisio, no Git connection),
  GitHub environment `production-database` (reviewer ChrilleInority, branch
  main; `SUPABASE_PROJECT_ID`, `SUPABASE_ACCESS_TOKEN`, `VERCEL_TOKEN`,
  `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`). `app.komisio.com` resolves to it.
- Done in the database: automation identity registered as billing actor, AI
  credits switched on (cap SEK 2 000, included 100, pack 100), the owner is
  platform host. Resend, SMTP and DNS were done by the owner.
- Still owner-only: Stripe live keys and the credit price ids per currency
  (Astra is on the Stripe product), Fortnox/Shopify/Zettle production
  registrations with production callback URLs, and replacing the personal
  Vercel and Supabase tokens in GitHub with dedicated ones. Done since this
  note was written: open e-mail delivery, the OpenAI reception assistant in
  both environments, the country block on new stores, and Komisio Print for
  production (`print-v1.0.1`), all on 2026-09-17.

## Marketing site (KomisioModern, Azure DevOps)

Branch `fable/free-and-open` (latest commit dd992f4) carries the new
content: free and open model, local currencies per locale, comparison table
(Serculate, Konsign, Crona, ConsignCloud, Ricochet), "New in Komisio"
section in eight languages, CTA to `https://app.komisio.com/register`,
terms section 3 rewritten (version 2.1). The owner uploads `out/` to Loopia
by FTP; merge the branch to main when the owner is satisfied. The pricing
page reads `https://app.komisio.com/api/public/pricing` live and shows real
store figures only when the host lists stores under Plattform.

## Next slices, in the order Fable would take them

1. **Native proofreading of the six new dictionaries.** Machine-translated
   from English; term choices the translators flagged are listed in the PR
   210 description and the agents' notes (voucher, onboarding slip, brief,
   custody, company suffixes in example names).
2. **Invitation allowlist for open registration.** `INVITATION_EMAIL_ALLOWLIST`
   only accepts exact addresses; production has the owner's address. Add a
   `*` (any address) value in `lib/platform/invitation-email.ts` and
   `lib/platform/seller-email.ts`, with a unit test, so stores can invite
   colleagues and e-mail sellers freely. Set it in Vercel production.
3. **Hosted MCP: the 18 excluded tools.** Their engine reads use table
   access; move those reads to SQL functions and remove them from
   `hostedExcludedTools` in `mcp/config.ts` (list and reasoning in
   docs/HOSTED-MCP.md). Then the owner tests Claude against production.
4. **Swish payouts** through the store's own bank agreement, no service fee
   (docs/PRICING.md, owner decision).
5. **Anthropic as a second own-key provider** for the reception assistant
   (`ai_connections.provider` currently allows only `openai`).
6. **A hosted copilot in the web interface** on the same credits (roadmap P4).

## How Fable worked, for whoever continues

- Worktree `C:\Workspace\Inority\komisio-fable` on the owner's machine; the
  `gh` CLI must use the stored git credential of ChrilleInority (the default
  `gh auth` is a Valmet account): `tok=$(printf 'protocol=https\nhost=github.com\n\n' | git credential fill | sed -n 's/^password=//p'); GH_TOKEN="$tok" gh ...`.
- Local database: apply a migration with `docker exec -i supabase_db_komisio psql -U postgres -d postgres -X -v ON_ERROR_STOP=1 < file`; run one pgTAP file the same way. 0059/0073/0094 fail locally on leftover fixture stores and pass in CI.
- A CASE inside a plpgsql `if ... then` must be parenthesised. A raise inside a function undoes its own updates (connector replay voiding is a separate call). Migration versions collide with Astra's: check origin/main before choosing one; next free is `20260916480000`, next pgTAP `0115`.
- The MCP harness fixture user gets MFA mid-run; the connector walk uses its own person.
- Secrets generated for production (credential key, cron secret, automation password, database password) exist only in Vercel and Supabase; nothing is in the repository.
